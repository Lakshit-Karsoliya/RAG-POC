Retrieval-Augmented Generation (RAG) is an architectural technique that enhances the capabilities of Large Language Models (LLMs) by augmenting them with external knowledge sources. Instead of relying purely on parametric memory learned during training, RAG retrieves relevant document snippets from a vector database like ChromaDB using vector embeddings.

This approach significantly reduces hallucinations and allows LLMs to cite up-to-date sources.